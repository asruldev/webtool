{
  "name": "webtool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://webtool.asrul.dev",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "deploy": "pnpm build && gh-pages -d dist",
    "deploy-only": "gh-pages -d dist",
    "predeploy": "pnpm build",
    "version:patch": "npm version patch --no-git-tag-version",
    "version:minor": "npm version minor --no-git-tag-version",
    "version:major": "npm version major --no-git-tag-version",
    "release:patch": "pnpm version:patch && pnpm build && git add . && git commit -m 'chore: bump version to v$(node -p \"require('./package.json').version\")' && git tag v$(node -p \"require('./package.json').version\") && git push && git push --tags",
    "release:minor": "pnpm version:minor && pnpm build && git add . && git commit -m 'chore: bump version to v$(node -p \"require('./package.json').version\")' && git tag v$(node -p \"require('./package.json').version\") && git push && git push --tags",
    "release:major": "pnpm version:major && pnpm build && git add . && git commit -m 'chore: bump version to v$(node -p \"require('./package.json').version\")' && git tag v$(node -p \"require('./package.json').version\") && git push && git push --tags",
    "push:deploy": "git add . && git commit -m 'feat: add crypto encoder/decoder tool' && git push",
    "tag:release": "git tag v$(node -p \"require('./package.json').version\") && git push --tags"
  },
  "dependencies": {
    "@types/crypto-js": "^4.2.2",
    "crypto-js": "^4.2.0",
    "prismjs": "^1.30.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.28.0",
    "react-simple-code-editor": "^0.14.1",
    "react-syntax-highlighter": "^15.6.1",
    "react-tooltip": "^5.29.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/prismjs": "^1.26.5",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/react-syntax-highlighter": "^15.5.13",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "eslint": "^9",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vite": "^5.4.0"
  }
}
